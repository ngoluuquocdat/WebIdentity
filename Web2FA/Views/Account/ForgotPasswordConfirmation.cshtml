<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Success</title>
    <style>
        /* body {
                    font-family: Arial, sans-serif;
                    background-color: #f4f4f4;
                    margin: 0;
                    padding: 0;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    height: 100vh;
                }
                */

        .container {
            text-align: center;
            background-color: transparent;
            padding: 20px;
            padding-top: 100px;
            font-family: "Lato", Arial, sans-serif;
        }

        .success-message {
            color: #6ec071;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .email-instruction {
            color: #fff;
            font-weight: 300;
            font-size: 24px;
            margin-bottom: 20px;
        }

        .email-icon {
            font-size: 100px;
            color: #6ec071;
            margin-bottom: 20px;
        }

        .note {
            color: rgba(255, 255, 255, 0.8);
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-message">Password recovery requested!</div>
        <div class="email-icon">&#9993;</div>
        <div class="email-instruction">Please check your email for the password reset action.</div>
        <div class="note">Note: The email may take a few minutes to arrive.</div>
    </div>
</body>
</html>
