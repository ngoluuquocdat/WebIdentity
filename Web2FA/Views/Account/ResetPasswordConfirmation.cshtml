<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Success</title>
    <style>

        .container {
            text-align: center;
            background-color: transparent;
            padding: 20px;
            padding-top: 100px;
            font-family: "Lato", Arial, sans-serif;
        }

        .success-message {
            color: #6ec071;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .email-instruction {
            color: #fff;
            font-weight: 300;
            font-size: 24px;
            margin-bottom: 20px;
        }

        .email-icon {
            font-size: 100px;
            color: #6ec071;
            margin-bottom: 20px;
        }

        .note {
            color: rgba(255, 255, 255, 0.8);
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-message">Password Updated!</div>
        <div class="email-icon">&#x2713;</div>
        <div class="email-instruction">Your password has been changed successfully.</div>
        <a asp-controller="Account" asp-action="Login" style="color: #e18c5c; cursor: pointer; font-size:18px" name="recover-submit" class="">&larr; Back to login</a>
    </div>
</body>
</html>
